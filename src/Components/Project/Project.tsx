import './Project.css'

type ProjectProps = {
    isFeaturedProject?: boolean,
    projectTitle: String,
    projectDesc: String,
    projectTags: number[]
}

export default function Project(props:ProjectProps){

   


    return(
        <div className="Project">
            
            
        </div>
    );
}